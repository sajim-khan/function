//function example

function getAverage(assignment1, assignment2, assignment3) {
    const sum = assignment1 + assignment2 + assignment3;
    const average = sum / 3;
    return average; 
}
var avgNo = getAverage(57,56,58);
console.log('My average assignment no is :',avgNo);